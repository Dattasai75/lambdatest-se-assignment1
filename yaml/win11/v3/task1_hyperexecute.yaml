version: 0.1
   runson: win11

   autosplit: true
   concurrency: 1

   runtime:
     language: java
     version: "11"

   env:
     TOKEN: anvdegtod-asdaasda0asda-asda

   pre:
     - mvn dependency:resolve

   testRunnerCommand: mvn test -Dplatname=win11 -Dmaven.repo.local=./.m2 -Dtest=SampleTest

   retryOnFailure: true
   maxRetries: 1

   jobLabel: [selenium-testng, win, v1]
